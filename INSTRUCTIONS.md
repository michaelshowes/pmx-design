1. Create project
```
npm create vite@latest pmx-mui -- --template react-ts 
```

2. Install MUI
```
npm install @mui/material @emotion/react @emotion/styled @mui/icons-material
```

3. Init Storybook with Playwright
```
npx storybook@latest init
```
